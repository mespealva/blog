<h1 class="h2 text-center">Abogados disponibles</h1>
<div class="text-center3 pt-4">
  <div class="row">
    <div class="col-6 offset-3 text-center">
      <% @lawyers.each do |lawyer| %>
        <div class="p-1"><%= link_to layer.name, lawyer_show_path(lawyer.id), class: 'lawyer' %></div>
      <% end %>
    </div>
  </div>
</div>